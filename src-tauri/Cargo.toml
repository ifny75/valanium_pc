[package]
# Версия клиента. Отсюда её берёт и Tauri (в tauri.conf.json её нет
# намеренно), и окно — командой app_version.
name = "valanium-windows"
version = "0.12.0"
edition = "2021"
rust-version = "1.80"
publish = false

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# Ядро линкуется напрямую: FFI здесь не нужен, обе стороны на Rust.
# Ядро живёт в своём репозитории. Ревизия пинится жёстко, а не веткой:
# клиент обязан собираться тем же ядром, которым его собрали и подписали,
# иначе «проверьте сами» перестаёт быть проверкой. Обновление ядра —
# отдельный осознанный коммит сюда.
valanium-core = { git = "https://github.com/ifny75/valanium_main", rev = "a557f8b8b06e74da17faa4aaadcc3971d7e6939c" }
tauri = { version = "2", features = ["tray-icon"] }
serde_json = "1"
# Разбор шестнадцатеричных строк: ключ и подпись манифеста обновлений.
hex = "0.4"
rand_core = { version = "0.6", features = ["getrandom"] }
windows = { version = "0.61", features = ["Win32_Foundation", "Win32_Security_Cryptography"] }
# Обработчик запросов разрешений WebView2. Версия совпадает с той, что уже
# тянет wry, — иначе в дереве оказались бы два несовместимых COM-слоя.
webview2-com = "0.38"
ureq = { version = "2", default-features = false, features = ["tls"] }
sha2 = "0.10"

[features]
# Проброс фичи ядра: платный вход в TON по умолчанию выключен.
ton = ["valanium-core/ton"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
